/**
 * atom-plan — Plan file management for pi-coding-agent
 *
 * Provides create_plan and edit_plan tools, tracks a "current plan" as session
 * state, and injects the plan's org content into context whenever it changes.
 *
 * TODO: implement (see PLAN.org)
 */

import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export default function (_pi: ExtensionAPI) {
  // Implementation coming soon — see PLAN.org
}
